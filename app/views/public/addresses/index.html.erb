<!--会員側の配送先画面-->
<div class="container my-5">
  <div class="row">
    <div class="col-7 m-auto">
      <h3>配送先登録/一覧</h3>

      <%= form_with model: @address do |f| %>
        <div>
          <div>
            <%= f.label :郵便番号, class: 'form-label' %>
            <%= f.text_field :customer_postcode, placeholder: '0000-0000', class: 'form-control mb-1' %>
          </div>
          <div>
            <%= f.label :住所, class: 'form-label' %>
            <%= f.text_field :customer_address, placeholder: '山形県山形市香澄町', class: 'form-control mb-1' %>
          </div>
          <div>
            <%= f.label :宛名, class: 'form-label' %>
            <%= f.text_field :customer_name, placeholder: '佐藤太郎', class: 'form-control mb-1' %>
          </div>
            <%= f.submit "新規登録", class: 'btn btn-sm btn-success mt-2' %>
        </div>
      <% end %>

      <!--配送先一覧-->
      <table class='table mt-4'>
        <thead>
          <tr>
            <th>郵便番号</th>
            <th>住所</th>
            <th>宛名</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
        <% @addresses.each do |address| %>
          <tr>
            <td><%= address.customer_postcode %></td>
            <td><%= address.customer_address %></td>
            <td><%= address.customer_name %></td>
            <td>
              <%= link_to "編集する", edit_address_path(address), class: 'btn btn-sm btn-primary' %>
              <%= link_to "削除する", address_path(address), method: :delete, class: 'btn btn-sm btn-danger' %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>